import { updatePassword } from "./actions";
export default function UpdatePasswordPage() { return <main className="mx-auto w-full max-w-md flex-1 px-5 py-20"><h1 className="text-4xl font-black tracking-tight">Nueva contraseña</h1><form action={updatePassword} className="mt-10 space-y-5"><label className="block text-sm font-semibold">Contraseña<input name="password" type="password" required minLength={8} autoComplete="new-password" className="mt-2 h-12 w-full border bg-white px-3 font-normal" /></label><button type="submit" className="h-12 w-full bg-stone-950 text-sm font-bold text-white hover:bg-accent">Guardar contraseña</button></form></main>; }
